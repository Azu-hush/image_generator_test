# Copy to .env or set in your shell. Never commit the real key.
# How to get one: docs/gemini-api-key.ru.md
GEMINI_API_KEY=paste_your_key_here
# Optional: delay between image requests in ms (rate limit safety)
GEMINI_DELAY_MS=1500
