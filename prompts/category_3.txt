CATEGORY 3 — object on a realistic surface. Object: {object}. The object rests on, or is set into, a realistic but simple {surface}. Behind it a plain vertical backdrop: {bg_color} with {bg_pattern}. The surface shows simple material detail matching its type; the backdrop stays flat and simple. Additional props: {props}.
